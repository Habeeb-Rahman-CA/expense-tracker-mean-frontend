export interface IUser {
    name?: string,
    email: string;
    password: string;
}

export interface IExpense {
    title: string;
    amount: number;
    category: string;
    date: string; 
    id: string;
}

